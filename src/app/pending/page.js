import TableComponent from '@/Components/TableComponent/TableComponent';
import React from 'react';

function page(props) {
    return (
        <div style={{height:'100%'}}>
            <TableComponent name={'Pending'}/>
        </div>
    );
}

export default page;